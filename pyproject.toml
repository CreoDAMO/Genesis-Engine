[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.14.3",
    "numpy>=2.4.6",
    "pytest>=9.1.1",
    "pyyaml>=6.0.3",
    "websockets>=17.0.1",
]
